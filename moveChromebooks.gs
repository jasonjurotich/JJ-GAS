function moveDevices() {
 var cb = {
  deviceIds: ['chromebook device id']
 };  
  var moveCb = AdminDirectory.Chromeosdevices.moveDevicesToOu(cb, 'id of domain', '/OranizationName/SubOrganizationName');
}
